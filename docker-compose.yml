services:
  api-server:
    image: ${DOCKER_IMAGE_PROD}
    container_name: flip-server-prod
    profiles: ["prod"]
    restart: always
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - FLIP_AI_API_URL=http://flip:8000/grade
    volumes:
      - ./logs:/logs
    networks:
      - flip-net

networks:
  flip-net:
    external: true
